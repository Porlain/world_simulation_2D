一、培训题目说明

　　　本系统主要是通过Web地图模拟地铁线路的流量情况，并将各个站点的实时流量以图形化的方式展现出来。系统需要一个Web端用于展示仿真的站点流量，一个流量模拟模块用于模拟实时流量。系统整体架构如图所示。 
![alt text](ui/b4d72d81-a4db-49c0-8abe-fda726695ace.png)

系统功能要求如下：

1.数据仿真功能：选择1条北京市地铁线路（也可以选其他城市的，一条线路即可），由流量模拟模块每隔1秒模拟生成该地铁线路上各个站点的流量（在给定的区间范围内生成随机数），各个站点流量的随机数范围可以在配置文件中进行设置，当模块首次运行时加载配置文件使用；

2.流量可视化展示：采用Web地图组件，在Web页面上将仿真的各个地铁站点的流量实时显示出来，站点流量的可视化显示方式由同学们自己设计，可以考虑用文字图层、柱状图、热力颜色等方式，请同学们开动脑筋，设计自己的可视化方案；

3.仿真控制功能：在Web系统界面上，需要设计实现仿真控制面板，提供仿真控制功能，主要包括：启动仿真、暂停仿真、结束仿真，三个功能。由Web系统的仿真控制面板提供用户交互，将用户命令传递给流量模拟模块，由流量模拟模块真正实现仿真的启动、暂停、结束功能；

4.仿真过程记录功能：Web系统可以将仿真过程数据记录并存储到数据库中，从仿真启动开始到仿真结束为一次有效仿真，系统应可以记录多次仿真的过程数据；

5.仿真回放功能：系统提供仿真回放功能，能够从数据库中取出存储的历次有效仿真数据并回放用户指定的仿真;

要求建立Git版本库，并使用版本库管理系统代码；
允许学生在现有功能上进行扩展；
允许学生使用大模型和Vibe Coding技术.
　

　　　数据仿真是科研中经常会遇到的任务，尤其是在交通领域，仿真更是经常需要用到的技术。本系统在训练Web系统设计开发能力的同时，对仿真系统的构建进行额外训练。Web系统采用的具体技术栈和框架由同学们自行设计制定，这里不做明确要求；流量模拟模块是一个基于定时器的独立运行程序，需要与Web系统进行通信和交互，具体实现架构、使用的编程语言也由同学们自行设计指定。

二、个人设计思路

### 总体思路

说到底核心就一件事：在一张 2D 地图上把人流情况直观地显示出来，其他功能都是围着这个转的。所以打算先把基础闭环做扎实，再谈扩展，不想一上来就铺得很大。

1.0 就拿一条线路的客流当仿真对象——站点对应地点，站间区间对应有向连接，要模拟和展示的就是各站点、各区间的实时客流。这么搭既贴合题目，底层又是个通用的网络模型，以后换到别的场景也不用推倒重来。

### 关于技术架构

优先选成熟、依赖少、和单机场景匹配的方案。

后端打算用 FastAPI，配 Pydantic 做参数校验。一开始也想过 Flask 和 Django：Flask 太朴素，校验和接口文档都得自己拼，写起来碎；Django 又太重，自带 ORM 和后台，对这种小接口反而是负担。FastAPI 基于类型注解自动校验、自动生成文档，异步也原生支持，和定时推进的后端比较合拍。官方有个专门对比其他框架的页面可以参考：https://fastapi.tiangolo.com/alternatives/

前端用 Vue3 加 TypeScript。React 也考虑过，老实说更多是熟悉度的差别——React 灵活但路由、状态、构建都要自己搭，Vue3 的组合式写法加官方配套开箱即用，这个规模的项目上手更快，性能和生态两边都够用。Vue 文档在 https://vuejs.org/ 。

地图这块纠结过一阵，最后选 Leaflet、不选 MapLibre。MapLibre 基于 WebGL、面向矢量瓦片，渲染强但重，主要服务在线瓦片地图；而本项目的地图是自造的本地平面坐标、压根不需要瓦片，Leaflet 轻得多、原生支持本地坐标绘图，正好够用。两边文档摆一起对比着看比较清楚：Leaflet 是 https://leafletjs.com/ ，MapLibre 是 https://maplibre.org/maplibre-gl-js/docs/ 。

数据库用 SQLite 单文件，不引入 MySQL 和 Redis。SQLite 零配置、自带事务，备份拷个文件就完事；MySQL 是客户端服务端那套，适合多用户并发，本地单用户用不上，还得额外养一个数据库进程；Redis 偏缓存和高速读写，每秒一次的写入根本到不了那个量级。SQLite 官方有个页面专门讲它什么时候适用、什么时候该换成 MySQL 这种，挺实在：https://www.sqlite.org/whentouse.html

前后端之间先用 HTTP 每秒轮询，没上 WebSocket。每秒取一份完整快照，轮询就够，简单又语义清楚；WebSocket 是给高频更新或服务端主动推送准备的，现在硬上就是给自己找麻烦。真要长连接再说，先翻一下 MDN 上 WebSocket 适用场景的介绍心里有数：https://developer.mozilla.org/en-US/docs/Web/API/WebSockets_API

数据访问直接写 SQL，不引 ORM。表就两张、关系又简单，SQLAlchemy 那层映射在这种规模下反而碍事，出问题不好查，等以后表复杂了再引也不迟。SQL 语法看 SQLite 官方的就够 https://www.sqlite.org/lang.html ，ORM 那边是 https://docs.sqlalchemy.org/ 。

定时调度用标准库的 asyncio，没上 Celery。Celery 是分布式任务队列，还得配 Redis 或 RabbitMQ 当中间件，适合多 worker 的重场景；本项目单进程一个后台任务就够，引它纯属多一份部署负担。Celery 文档 https://docs.celeryq.dev/ 和 asyncio 文档 https://docs.python.org/3/library/asyncio.html 摆一起，差别一目了然。

### 数据相关

场景和运行数据是分开存的，这个区分是有意的。场景写成一个 JSON 文件，站点、区间连接、各流量的取值范围都放里头——它适合进 git，结构直观，对比和复用都方便。

运行过程的数据就不一样了，要按时间查、要加约束（比如同一时刻只准一个活动仿真）、还要事务保证，这些拿 JSON 文件做很别扭，所以丢进 SQLite。库里其实两张表就够了：一张记每次仿真的基本信息（用的哪个场景、随机种子、当前状态、起止时间），另一张按时间点存每一份快照。

有个细节比较重要：场景文件以后肯定会改，但老回放不能跟着错位。所以每次启动仿真时，把当时的场景连同它的校验值一起塞进这次运行记录，回放就只读当时存下来的那份，后来的改动影响不到它。

数据库操作直接写 SQL，没引 ORM。表就两张、关系又简单，多套一层映射反而碍事，等以后表真复杂了再说。模块这边把地图数据、流量生成、数据记录各自分开，保持解耦，后面想替换哪块都好动。

### 仿真引擎相关

流量模拟模块就是个基于定时器自己往前推的过程，每隔一秒走一个时间步（暂时够用），每个站点按配置的区间生成人流或车流，区间范围写在配置文件里，模块启动时加载。

随机数这块特意做成确定性的——不用普通的随机发生器，而是拿固定的种子、当前时间步、再加上具体的连接和流量类型一起推导出一个落在区间里的数。好处很直接：种子和场景一样，每一步的数就完全一样，结果能逐拍复现，调试和回放核对都省心。普通随机不行，它受调用顺序影响，中间多一步少一步后面就全乱了，根本没法对账。

人到下一站不是瞬间传送，路上的时间得体现出来。打算给每条连接配一个旅行时间，用一个先进先出的小队列把刚出发的人往后排，过了相应的拍数才到目的地——这样流量在地图上是逐步挪过去的，而不是一出发就闪现。

还有个分流的细节：某个站点能走的人数不够所有方向的需求时，就按比例公平分一下，各方向尽量都给到，但总数不能超过实际可用的人。

整个计算只做纯计算，不碰数据库、不碰网络和界面。这样它好单独拎出来测，也好保证每次跑出来一致。计算上只盯着各站点和各区间的聚合人数，每种流量类型的总量前后必须守恒，不能凭空多出来或者凭空没了。

### ui控制相关

界面上提供启动、暂停、结束几个操作，命令走接口交给后端的仿真模块去真正执行。

这里特意把定时器和推进逻辑都放在后端进程里，让它独立于浏览器跑——正好对应题目说的"基于定时器的独立运行程序"。这样浏览器关了仿真照常推进，重新打开页面只是重新去查看，不会把仿真打断。

同时只跑一次仿真比较稳妥，所以打算在两层上卡：程序内存里只留一个活动仿真，数据库那边再加一条约束兜底，万一程序自己判断错了，也不至于冒出两条活动仿真抢着写数据。

状态在运行、暂停、结束、异常终止之间切换，每次变都先看当前状态对不对（比如已经结束的就不能再暂停），没问题才写进去，免得并发命令把状态改乱。所有控制命令都是先成功落库、再回给前端，前端看到的永远是已经存下来的状态。

后端要是意外挂了，重启时会把上次没正常收尾的仿真标成异常终止，免得留个半拉子状态造成数据对不上。

### 可视化细节

地图用本地坐标，不走经纬度那一套世界坐标。

站点画成圆点，大小直接反映客流密度；区间用线段连起来，颜色按繁忙程度分几档，比如通畅、繁忙、接近满载。

人流是流动的、不是钉死在原地，所以前端打算再叠一层粒子，体现那种流动感，可以参考飞机航线图上动态点的画法。不过这些粒子纯粹是视觉示意，数量是按各区间的在途人数按比例抽样出来的，并不真对应某个人，也不写进数据库——真数还是看快照里的聚合值。

颜色之外，再用线宽和圆点大小一起传达信息，尽量让不同视觉条件下都读得清楚，不只靠颜色一种维度。密度高的地方，也可以考虑叠一层热力图。

### 时间轴与回放

回放这块，想用一条时间轴把实时查看和历史回放一起兜了：运行时跟着最新进度走，想看历史就拖过去，仿真结束后同一条轴接着当回放用，不再单独搞个实时/回放的切换。

所以这么干，主要是不想维护两套各管各的逻辑和状态，容易乱。回放的时候就是按时间点把存好的快照取出来显示，不重新算，也不产生新数据——看到的就是当时记下来的样子，原汁原味。

### 后续扩展

基础功能稳了之后，再考虑往多了扩。比如接个大模型来生成随机事件——好的事件把人吸引过去，坏的事件让人四散——再给人群分个类（官方组织赶去处置、民众撤离、商人逐利之类）。不过这部分希望做成可选的，别一上来就全自动跑，留个人能干预的口子。

世界层面那些设定，不同种族、世界地图生成、城镇放大钻取，目前就当设计参考，先不进实现，等 1.0 稳住了再一步步想。

外部开源项目这块，倾向用数据导入的方式借鉴，不直接去改人家代码；真要引什么外部资源，先把它许可证确认清楚再说。

### 参考资料

关于世界、种族、怪物等设定，目前主要参考相关资料中的名称和整体思路，不做完整照搬，毕竟本系统的核心是流量显示而非完整的游戏。

- 世界设定参考：/share_data/songzun/classes/software/world/1、DND_5E_城主指南CN.pdf
- 种族设定参考：/share_data/songzun/classes/software/people/2、DND_5E_玩家手册CN.pdf
- 怪物设定参考：/share_data/songzun/classes/software/monsters/3、DND_5E_怪物图鉴CN.pdf

此外，在地图显示、世界与城镇生成以及流量可视化等方面，也参考了一些开源项目，后续若实际引入会先确认其相应的许可证：

- 地图显示参考 Leaflet：https://leafletjs.com/
- 世界地图生成参考 Fantasy Map Generator：https://github.com/Azgaar/Fantasy-Map-Generator
- 城镇生成参考 Medieval Fantasy City Generator：https://watabou.github.io/city.html
- 流量流向可视化参考 FlowmapBlue：https://github.com/FlowmapBlue/FlowmapBlue
